import 'package:flutter/material.dart';

/// Paleta centralizada (a mesma do site)
class DSColors {
  static const primary       = Color(0xFFEC4899);
  static const primaryDark   = Color(0xFFDB2777);
  static const primaryLight  = Color(0xFFFBCFE8);
  static const secondary     = Color(0xFF7E22CE);

  static const text          = Color(0xFF1F2937);
  static const textLight     = Color(0xFF6B7280);

  static const success       = Color(0xFF10B981);
  static const danger        = Color(0xFFEF4444);
  static const warning       = Color(0xFFF59E0B);

  static const background    = Color(0xFFF9FAFB); // cinza bem claro
  static const surface       = Colors.white;
  static const border        = Color(0xFFE5E7EB);
}
